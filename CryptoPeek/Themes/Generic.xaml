<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:controls="clr-namespace:CryptoPeek.Controls"
                    xmlns:converters="clr-namespace:CryptoPeek.Converters">

    <converters:CollapsedHeightConverter x:Key="collapsedHeightConverter" />


    <Style TargetType="{x:Type controls:ExpandableTextBlock}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type controls:ExpandableTextBlock}">
                    <StackPanel>
                        
                        <TextBlock x:Name="preview_Text" 
                                   Text="{TemplateBinding Text}"
                                   TextWrapping="Wrap"
                                   TextTrimming="CharacterEllipsis">
                            <TextBlock.MaxHeight>
                                <MultiBinding Converter="{StaticResource collapsedHeightConverter}">
                                    <Binding RelativeSource="{RelativeSource TemplatedParent}" Path="IsExpanded" />
                                    <Binding RelativeSource="{RelativeSource TemplatedParent}" Path="CollapsedLines" />
                                    <Binding ElementName="preview_Text" Path="FontSize" />
                                </MultiBinding>
                            </TextBlock.MaxHeight>
                        </TextBlock>

                        <ToggleButton IsChecked="{Binding IsExpanded, 
                                                  RelativeSource={RelativeSource TemplatedParent}, 
                                                  Mode=TwoWay}"
                                      Width="16" 
                                      Height="16"
                                      Background="Transparent"
                                      BorderThickness="0"
                                      HorizontalAlignment="Left"
                                      Margin="0,4,0,0"
                                      Focusable="False">
                            <ToggleButton.Template>
                                <ControlTemplate TargetType="ToggleButton">

                                    <Grid>

                                        <Ellipse Stroke="Gray"
                                                 StrokeThickness="1"
                                                 Fill="White" />

                                        <Viewbox Stretch="Uniform"
                                                 Margin="3">

                                            <Path x:Name="Arrow"
                                                  Data="M 0 0 L 4 4 L 8 0"
                                                  Stroke="Gray"
                                                  StrokeThickness="1.5"
                                                  HorizontalAlignment="Center"
                                                  VerticalAlignment="Center"
                                                  Stretch="Uniform"
                                                  RenderTransformOrigin="0.5,0.5">
                                                <Path.RenderTransform>
                                                    <RotateTransform Angle="0"/>
                                                </Path.RenderTransform>
                                            </Path>

                                        </Viewbox>

                                        <VisualStateManager.VisualStateGroups>
                                            <VisualStateGroup x:Name="CheckStates">
                                                
                                                <VisualState x:Name="Checked">
                                                    <Storyboard>
                                                        <DoubleAnimation Storyboard.TargetName="Arrow"
                                                                         Storyboard.TargetProperty="(Path.RenderTransform).(RotateTransform.Angle)"
                                                                         To="-180"
                                                                         Duration="0:0:0.2"/>
                                                    </Storyboard>
                                                </VisualState>
                                                
                                                <VisualState x:Name="Unchecked">
                                                    <Storyboard>
                                                        <DoubleAnimation Storyboard.TargetName="Arrow"
                                                                         Storyboard.TargetProperty="(Path.RenderTransform).(RotateTransform.Angle)"
                                                                         To="0"
                                                                         Duration="0:0:0.2"/>
                                                    </Storyboard>
                                                </VisualState>
                                                
                                            </VisualStateGroup>
                                        </VisualStateManager.VisualStateGroups>

                                    </Grid>

                                </ControlTemplate>
                            </ToggleButton.Template>
                        </ToggleButton>
                    </StackPanel>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>